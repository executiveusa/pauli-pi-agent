{
  "mcpServers": {
    "context-mesh": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/bin/context-mesh.mjs"],
      "env": {
        "CONTEXT_MESH_GRAPH": "${CLAUDE_PROJECT_DIR}/graphify-out/graph.json"
      }
    }
  }
}
